# FieldDay Backend Environment Variables

# Server
PORT=4000
NODE_ENV=development

# JWT Secrets (CHANGE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-me-in-production

# Redis (optional - uses in-memory fallback if not set)
REDIS_URL=redis://localhost:6379

# Application URL (for magic links)
APP_URL=http://localhost:3000

# Twilio SMS (optional - logs to console if not set)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+15551234567

# SendGrid Email (optional - logs to console if not set)
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_FROM_EMAIL=noreply@fieldday.com

# Database (if using PostgreSQL instead of in-memory)
# DATABASE_URL=postgresql://user:password@localhost:5432/fieldday

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:19006
